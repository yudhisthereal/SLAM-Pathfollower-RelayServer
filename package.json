{
  "name": "robot-relay-worker",
  "version": "1.0.0",
  "description": "WebSocket relay for robot teleoperation on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}